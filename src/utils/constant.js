import 'dotenv/config'
  
  
export const {
    SERVER_PORT,
    SERVER_HOST,
    DATABASE_URL,
    DB_PASSWORD,
    DB_HOST,
    DB_PORT,
    DB_USER,
    DB_NAME,
    EMAIL_USER,
    EMAIL_PASS,
    EMAIL_NAME,
    JWT_PRIVATE_KEY,
    JWT_ACCESS_EXPIRATION_TIME,
    JWT_REFRESH_EXPIRATION_TIME

} = process.env